# macOS → Batocera deploy config (ServoStik)
GITHUB_REPO="https://github.com/jdruding/Batocera-Ultimarc-ServoStik-Conrol.git"
GIT_REF="main"

# Batocera SMB target
BATOCERA_HOST="batocera.local"   # or the Pi's IP address
BATOCERA_SHARE="share"
MOUNT_POINT="/Volumes/BATOCERA"

# SMB auth (leave blank for guest or interactive prompt)
SMB_USER=""
SMB_PASS=""

# Behavior
RSYNC_DELETE=        # set to 1 to mirror deletes
# DRY_RUN=1          # uncomment for dry run

# (Optional) Ensure execute bit via SSH on Batocera
DO_CHMOD_SSH=        # set to 1 to enable SSH chmod
SSH_USER="root"
SSH_PORT="22"
