# ==========================================
# Batocera ServoStik Deploy Config (macOS)
# ==========================================

# --- GitHub source repo ---
# URL of the GitHub repo to deploy from
GITHUB_REPO="https://github.com/jdruding/Batocera-Ultimarc-ServoStik-Conrol.git"
# Branch, tag, or commit
GIT_REF="main"

# --- Batocera target ---
# Hostname or IP of your Batocera box
# Example: "BATOCERA3" or "192.168.1.50"
BATOCERA_HOST="batocera.local"
# Share name (Batocera usually exports "share")
BATOCERA_SHARE="share"

# --- Mount point on macOS ---
# Default is a folder in your home directory (safe and writable)
MOUNT_POINT="$HOME/BATOCERA"
# You can also set to "/Volumes/BATOCERA" if you prefer, but that may require sudo

# --- SMB credentials ---
# Leave blank to try guest access, or set your Batocera SMB username/password
SMB_USER=""
SMB_PASS=""

# --- Rsync behavior ---
# Set to 1 to mirror deletes (destination matches repo exactly, extra files removed)
RSYNC_DELETE=
# Set to 1 for dry-run (no writes, just show what would happen)
# DRY_RUN=1

# --- Permissions fix via SSH ---
# Set to 1 to log into Batocera over SSH and force chmod +x on the game-start script
DO_CHMOD_SSH=
SSH_USER="root"
SSH_PORT="22"
